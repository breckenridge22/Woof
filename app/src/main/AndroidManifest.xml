<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.osu.cse.apps.mobile.woof">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeScreenActivity">
        </activity>
        <activity android:name=".DogSelectionActivity"></activity>
        <activity android:name=".DogManagementActivity"></activity>
        <activity android:name=".SettingsActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".NewDogActivity"></activity>
        <activity android:name=".DeleteActivity"></activity>
        <activity android:name=".MapsActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".FamilyMainActivity"></activity>
        <activity android:name=".FamilySelectionActivity"></activity>
        <activity android:name=".FamilyManagementActivity"></activity>
        <activity android:name=".FamilyInvitationsActivity"></activity>
        <activity android:name=".CreateNewFamilyActivity"></activity>
        <activity android:name= ".MultiDogSelectionActivity"></activity>
        <activity android:name=".NewActivityRecordActivity"></activity>


    </application>

</manifest>